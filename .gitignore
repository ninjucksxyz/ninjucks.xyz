# Rust build output (any crate, any depth)
target/
**/*.rs.bk

# Built artifacts (wasm is reproducible from source)
artifacts/mock_router.wasm
contract.wasm
hash.txt

# Secrets / local env — MUST never be committed
.env
.env.*
!.env.example

# Wrangler / Cloudflare local state
.wrangler/
node_modules/

# built SDK bundle (generated by build/ in CI)
frontend/vendor/
build/node_modules/

# local deploy/test scripts — not published
scripts/
